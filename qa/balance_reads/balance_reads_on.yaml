overrides:
  ceph:
    conf:
      global:
        enable experimental unrecoverable data corrupting features: '*'
        rados_replica_read_policy: balance
      osd:
        rados_replica_read_policy: balance